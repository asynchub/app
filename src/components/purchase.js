import React, { Component } from 'react';

import Organizer from './organizer';

export default class Purchase extends Component {
  render() {
    return (
      <div>
        <p>Purchase is routed component to manage purchases, using realtime data</p>
      </div>
    );
  }
}
