import React, { Component } from 'react';

export default class LoginForm extends Component {
  render() {
    return (
      <div>Login form to navigate route according to user profile</div>
    );
  }
}
