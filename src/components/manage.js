import React, { Component } from 'react';

import Organizer from './organizer';

export default class Manage extends Component {
  render() {
    return (
      <div>
        <p>Manage is routed component to reflect realtime statistics.</p>
      </div>
    );
  }
}
