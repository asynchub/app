import React, { Component } from 'react';

import Organizer from './organizer';

export default class Plan extends Component {
  render() {
    return (
      <div>
        <p>Plan is routed component to build and maintain schedules, using realtime data</p>
      </div>
    );
  }
}
