import React, { Component } from 'react';

export default class Organizer extends Component {
  render() {
    return (
      <div>
        <p>Organizer to have realtime Chat and Calendar</p>
      </div>
    );
  }
}
